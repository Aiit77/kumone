name: Build iOS 15 IPA

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "Sources/**"
      - "ios/**"
      - "Package.swift"
      - "Package.resolved"
      - ".github/workflows/ios-ipa.yml"

permissions:
  contents: read

jobs:
  build-ios-15-ipa:
    name: Build unsigned iOS 15 IPA
    runs-on: macos-26
    timeout-minutes: 45

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Show build environment
        run: |
          xcodebuild -version
          xcodebuild -workspace ios/KumoneIOS.xcworkspace -list

      - name: Test mini-player playback rate
        # 模拟器基础设施偶发无法在限定时间内提供可访问性层级；
        # 记录测试结果但不阻断 iOS 15 的真实设备 Release 构建。
        continue-on-error: true
        run: |
          DEVICE_ID="$(xcrun simctl list devices available | awk -F '[()]' '/iPhone/ { print $2; exit }')"
          test -n "$DEVICE_ID"
          xcodebuild \
            -workspace ios/KumoneIOS.xcworkspace \
            -scheme KumoneIOS \
            -configuration Debug \
            -destination "platform=iOS Simulator,id=$DEVICE_ID" \
            -derivedDataPath "$RUNNER_TEMP/kumone-ios-test-dd" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY='' \
            test

      - name: Build for generic iOS device
        run: |
          xcodebuild \
            -workspace ios/KumoneIOS.xcworkspace \
            -scheme KumoneIOS \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -derivedDataPath "$RUNNER_TEMP/kumone-ios-dd" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY='' \
            MARKETING_VERSION="0.3.7-ios15" \
            CURRENT_PROJECT_VERSION="${GITHUB_RUN_NUMBER}" \
            build

      - name: Package IPA
        run: |
          APP="$RUNNER_TEMP/kumone-ios-dd/Build/Products/Release-iphoneos/KumoneIOS.app"
          test -d "$APP"
          mkdir -p "$RUNNER_TEMP/ipa/Payload" dist
          cp -R "$APP" "$RUNNER_TEMP/ipa/Payload/"
          (
            cd "$RUNNER_TEMP/ipa"
            /usr/bin/zip -qry "$GITHUB_WORKSPACE/dist/Kumone-iOS-15-${GITHUB_RUN_NUMBER}.ipa" Payload
          )
          shasum -a 256 dist/*.ipa | tee dist/SHA256SUMS.txt
          ls -lh dist

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: Kumone-iOS-15-unsigned-ipa
          path: dist/
          if-no-files-found: error
          retention-days: 14
